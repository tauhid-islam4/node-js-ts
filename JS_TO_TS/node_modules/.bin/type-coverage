#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../type-coverage/bin/type-coverage" "$@"
  ret=$?
else 
  node  "$basedir/../type-coverage/bin/type-coverage" "$@"
  ret=$?
fi
exit $ret
